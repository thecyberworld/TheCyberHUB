import React, { useState } from "react";
import Categories from "./Categories/Categories";

const Quiz = () => {
  return (
    <section>
      <Categories />
    </section>
  );
};

export default Quiz;
