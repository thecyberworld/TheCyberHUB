import React from "react";
import { ExpContainer, ExpIcon, ExpText } from "./ExpElemenets";

const Exp = () => {
    return (
        <ExpContainer>
            <ExpText> 1 </ExpText>
            <ExpIcon />
        </ExpContainer>
    );
};

export default Exp;
