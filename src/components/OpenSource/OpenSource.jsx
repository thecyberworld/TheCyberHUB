export default function OpenSource() {
  return (
    <section
      style={{
        height: "100vh",
        display: "flex",
        justifyContent: "center",
        alignItems: "center",
        padding: "3rem",
      }}
    >
      <h1 style={{ color: "white", fontSize: "5rem", textAlign: "center" }}>
        Opensource Projects will display here.
      </h1>
    </section>
  );
}
